configuration {
    /* Optional: Global settings that don't affect the layout */
    modi: "drun,run";
    show-icons: true;
    icon-theme: "Papirus"; 
    /* Font is handled in colors.rasi, but you can override it here if you want */
}

/* This loads the file generated by Wallust, which contains all your layout and colors */
@theme "~/.config/rofi/colors.rasi"
